"""Deprecated — replaced by ``exporter/phases/describe/describe.py``.

The Phase 1 orchestrator has moved to ``describe_scene`` in the new
location. This file remains as an empty stub because the surrounding
``describe_blender/helpers/`` directory still hosts the deep-work
decoders for materials, animations, and material animations that
``describe/helpers/`` shells delegate to. Once those decoders migrate
into ``describe/helpers/_decode/``, the whole ``describe_blender/``
tree can be deleted.
"""
